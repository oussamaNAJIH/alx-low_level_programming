#include <stdio.h>
/**
 * main - Displays the alphabet without q and e.
 * Return: return 0 (Success)
 */
int main(void)
{
int i;
for (int i = 0; i <= 9; i++)
{
printf("%d", i);
}
putchar('\n');
return (0);
}
