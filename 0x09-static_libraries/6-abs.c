#include "main.h"
/**
 * _abs - checks the sign of a num.
 * @n: number that on intend to calculate the absolut value of
 * Return: return the absolute value of the argument
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
